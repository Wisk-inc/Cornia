import { MaxPage } from "../components/MaxPage"

export const metadata = {
	title: "Cornia Max — every model, every tool",
	description:
		"Every GPT model, a sandbox terminal and workspace, Deep Research, unlimited image generation, and Cornia Code on your GitHub repositories.",
}

export default function Page() {
	return <MaxPage />
}
